[build]
  command = "npm run build"
  publish = "dist/portfolio/browser"

[build.environment]
  NETLIFY_USE_YARN = "false"

[[plugins]]
  package = "@netlify/plugin-lighthouse" # optional, safe plugin

# 👇 This disables Angular runtime auto-detection
[functions]
  node_bundler = "esbuild"